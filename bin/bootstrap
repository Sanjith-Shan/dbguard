#!/usr/bin/env python3
"""bin/bootstrap, see dbguard/harness/bootstrap.py.

Orchestrator profile only: never run it while the dbguard manager container is up.
"""
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), ".."))

from dbguard.harness.bootstrap import main

sys.exit(main())
